# Testing Dependencies for Community Brief Backend
# Install with: pip install -r requirements-test.txt

# Core testing framework
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-mock==3.15.1
pytest-cov==7.1.0

# HTTP testing
httpx==0.28.1

# JWT for authentication testing
python-jose[cryptography]==3.5.0

# Azure SDK mocking (already in main requirements but listed for clarity)
# Keep Azure SDK dependencies in `backend_app/requirements.txt` to avoid duplicate, potentially conflicting pins.

# Runtime/development helpers used during tests
# fastapi==0.100.0  # Removed to avoid conflict with requirements.txt
# On Windows, python-magic-bin bundles libmagic. On Linux/macOS, use python-magic.
python-magic==0.4.27

# Code coverage reporting
coverage[toml]==7.13.5

# Additional test utilities
faker==40.15.0  # For generating test data
freezegun==1.5.5  # For time-based testing
